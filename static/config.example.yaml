server:
  host: 127.0.0.1
  port: 8080
redis:
  host: redis
  port: 6379

services:
  # auth required
  internal:
    dumb:
      path: dumb-service
      host: dumbservice
  external:
    dumber:
      path: dumb-service 
      host: dumbservice/external
server:
  host: 127.0.0.1
  port: 8080
redis:
  host: redis
  port: 6379

services:
  # auth required
  internal:
    - path: dumb-service
      host: localhost:8000
  external:
    - path: dumb-service 
      host: localhost:8000/external